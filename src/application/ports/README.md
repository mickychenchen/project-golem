# `src/application/ports`

Application-layer ports (interfaces/contracts) for use-cases.

Guidance:
- Define dependency contracts as plain JS shapes (duck typing) with JSDoc.
- Keep adapters in `src/infrastructure`.
