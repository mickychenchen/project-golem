# `packages/memory`

Memory package.

- `LanceDBProDriver`
- `SystemNativeDriver`
- `ExperienceMemory`
- `embeddings`

Current status:
- ✅ `LanceDBProDriver`, `SystemNativeDriver`, and `ExperienceMemory` implementations now live in this package.
- ✅ `embeddings` implementation now also lives in this package (`packages/memory/embeddings`).
- ✅ Legacy shim files have been removed from `src/`.
